#!/usr/bin/env python
# encoding: utf-8

default:
  target: dev
  outputs:
    dev:
      type: doris
      host: [host]
      port: [port] # optional
      username: [username]
      password: [password]
      schema: [schema]
    prod:
      type: doris
      host: [host]
      port: [port] # optional
      username: [username]
      password: [password]
      schema: [schema]
